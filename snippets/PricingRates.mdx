{/*
  Rebuilt from the original interactive PricingRates.tsx (a per-second /
  per-hour toggle over a shadcn Switch, sourced from the HOURLY_RATES
  constant: cpu 0.07, ram 0.04375, storageHot 0.5, storage 0.02 $/hour).
  Mintlify snippets don't carry that component library, and a rate table
  doesn't gain much from the toggle, so this is a static per-second table —
  the granularity Sprites actually bills at. If the rates change, recompute
  hourly/3600 for the first two rows.
*/}

| Resource | Rate (per second) | Notes |
|---|---|---|
| **CPU Time** | `$0.00001944` / vCPU-second | |
| **Memory Time** | `$0.00001215` / GB-second | |
| **Hot Storage** | `$0.50` / GB-month | Local NVMe cache for active working data, sampled every few seconds. |
| **Storage** | `$0.02` / GB-month | Object storage for persistent data, measured hourly. |

*All compute resources are billed per second. Hot storage is sampled every few seconds while active; storage is measured hourly.*
