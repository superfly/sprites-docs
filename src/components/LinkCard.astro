---
import { LinkCard as LinkCardReact } from './react';

interface Props {
  href: string;
  title: string;
  description: string;
  icon?: string;
  external?: boolean;
}

const props = Astro.props;
---

<LinkCardReact {...props} client:load />
